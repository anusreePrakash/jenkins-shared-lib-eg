tool:
  git:
    installations:
      - name: git
        home: /usr/local/bin/git

credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: SYSTEM
              id: jcasc_try
              username: my_username
              passphrase: some_passworrd
              description: "random passphrase"
